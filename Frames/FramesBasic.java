package Frames;

public class FramesBasic {
	
	/*
	 * Displaying a web page as a part of another page is frame.
	 * Iframe is created by using tagname iframe.
	 * We can switch to iframe by using 3 ways:-Id Name Index
	 *
	 *Handle iframe using selenium webdrier:-
	 *To handle iframe we need to switch selenium foucus from main page to frame
	 *driver.switchTo().frame("Frame Id, Name, Index")
	 *
	 *Selenium will not navigate by default to main page
	 *For navigation from iframe to main page or parent page:-
	 *driver.switchTo.parentFrame():- It will navigate from child to immediate parents
	 *driver.switchTo.defaultContant():-It will navigate from child to main page
	 * 
	 */
}
